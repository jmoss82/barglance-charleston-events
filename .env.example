# Copy to .env for local reference, or set these in your host (e.g. Vercel).
# The app reads keys from config.js in the browser; use this file as a checklist.
# To run locally: copy config.example.js to config.js and fill in the values there.

MAPBOX_ACCESS_TOKEN=your_mapbox_public_token
BARGLANCE_PARTNER_API_KEY=your_barglance_partner_api_key
